# xpelevator Documentation

This directory is governed by the Factory documentation control plane.

Start with:

- `docs/_catalog/agent-truth-map.json` for agent trust rules.
- `docs/_governance/canonical-docs.yml` for canonical docs.
- `docs/_governance/doc-overrides.yml` for status and owner routing.

Generated catalog outputs are produced by the shared Factory docs-health workflow.
