# XPElevator — Requirements

> The real and true requirements this build is held accountable to. Numbered,
> testable, statused. Every control-issue (#16) slice references the requirement
> IDs it advances; every `live` flip needs verification evidence in the #16
> comment. Status ∈ `live | building | roadmap | rejected | parked`. `rejected`
> rows stay here with their reason — rejected scope is a guardrail.
>
> Verification note: this repo's sandbox cannot curl the live branded domain
> directly (egress allowlist), so `curl` evidence for `live` rows is produced by
> the GitHub Actions runner — `deploy.yml` post-deploy gates and
> `uptime-monitor.yml` — which is the authoritative live check for every slice.

## Functional

| ID | Requirement | Status | Verification |
|---|---|---|---|
| R-001 | A trainee can run a **chat** simulation against an admin-authored scenario and receive a streamed customer response | live | `/api/chat` SSE; ui test `tests/ui/simulate-page`; canary session drives full chat |
| R-002 | A completed session produces a **non-null /10 score** against weighted per-role criteria | live | 6 h scoring canary (uptime-monitor.yml) → session `dad64d13` 7/7 non-null, avg 6.86 |
| R-003 | A trainee can run a **browser voice** simulation (push-to-talk + hands-free) | live | Web Speech path in `src/app/simulate`; manual |
| R-004 | A trainee can run a **real phone** simulation via Telnyx | live | `/api/telnyx/call` + `/api/telnyx/webhook`; `src/lib/telnyx.ts` |
| R-005 | An org admin can CRUD job titles, criteria, scenarios, and job↔criteria links, org-scoped | live | `/api/jobs`,`/api/criteria`,`/api/scenarios`; admin UI |
| R-006 | The platform is multi-tenant: rows are org-scoped and one org cannot read another's sessions | live | `canAccessSession` (owner-or-same-org); deploy gate: anon 401 / cross-tenant 403 / owner 200 (run 28755424092) |
| R-007 | A public seat-plan catalog is served for the operator pricing surface | live | `GET /api/plans` (PR #36); unit tests `src/lib/plans.ts` |

## Security

| ID | Requirement | Status | Verification |
|---|---|---|---|
| R-020 | Every `/api/*` read except the public allowlist requires authentication | live | deploy gate: anon `/api/{scenarios,jobs,criteria}` → 401 (run 28745544626) |
| R-021 | Scenario **hidden hints / persona / objective are never returned to non-owners** (the core product mechanic) | live | deploy gate: anon `/api/scenarios` body has no `hints`/`customerObjective`/`customerPersona` |
| R-022 | No IDOR on session reads: cross-tenant `GET /api/chat?sessionId=` is blocked | live | deploy gate: cross-org read → 403 for JSON + `?stream=true` (PR #28/#29) |
| R-023 | Security headers (CSP, HSTS, nosniff, X-Frame-Options DENY, Referrer/Permissions-Policy) on every response | live | deploy gate on live domain + pages.dev (PR #30, run 28759891246) |
| R-024 | No secrets in source or `wrangler.toml`; app secrets are CF Pages secrets | live | grep of repo; `wrangler.toml` carries placeholders only |

## Experience (Phase E — conversation realism)

Founder directive (#16, 2026-07-10): the conversation "is currently a half speed sparring session vs a real life simulation." Phase E-root #2: voice mode waited for the full model reply before speaking a single word (TTS fired only on the SSE `done` event) → multi-second dead air every turn.

| ID | Requirement | Status | Verification |
|---|---|---|---|
| R-030 | Voice mode speaks the customer reply **incrementally** — first audio starts on the first complete sentence as the stream arrives, not after the whole response | live | pure boundary logic `src/lib/speech.ts` (unit: `tests/unit/lib/speech.test.ts`, 100% lines); `useChatSession` emits `speechChunks` per sentence; `VoiceChatInterface` speaks each as it lands. Listen-test: multi-sentence reply begins audibly before the reply finishes |
| R-031 | The simulated customer's **emotional state is fixed for the whole session** (deterministic from scenario + session id), not re-rolled per turn — a "hard" customer no longer flip-flops angry↔frustrated between messages | live | the system prompt is rebuilt every turn (chat + telnyx routes), so `src/lib/ai.ts` selects the mood deterministically via `stableIndex` (replaces `Math.random()`); routes pass `sessionId` as the seed. Unit `tests/unit/lib/ai.test.ts`: mood stable across 25 rebuilt turns, `Math.random` never called, variety preserved across sessions |
| R-032 | The simulated customer has **one coherent name**, owned by the scenario persona — the prompt no longer injects a name from a fixed pool alongside the persona's own name (which produced two conflicting names in one prompt) | live | `src/lib/ai.ts` — the `CUSTOMER_NAMES` pool is removed; `buildCustomerSystemPrompt` lists only the persona under identity and instructs the customer to hold one fixed name (the persona's if named, else self-chosen) for the whole call. Unit `tests/unit/lib/ai.test.ts` (E-root #4): no retired-pool name leaks, no `- Name:` line, persona-supplied name surfaces, fixed-name directive present |
| R-033 | **Scoring never silently discards a session's scores.** A recoverable defect in the judge model's JSON (leading/trailing prose, markdown fences, a bare object, one malformed row, truncation) must still yield every well-formed score rather than an unexplained zero; a scorable session that genuinely produces no scores is surfaced to the client as `scoringFailed`, not persisted as a silent zero | live | `src/lib/ai.ts` — pure `parseScoreRows()` recovers rows via whole-parse → first balanced `[…]` substring → per-object salvage; `scoreSession` uses it and logs distinctly on total failure. `src/app/api/chat/route.ts` `endSession` returns `scoringFailed` when a scorable session yields zero scores. Unit `tests/unit/lib/ai.test.ts` (E-root #8): recovers prose-wrapped/fenced/bare-object JSON, salvages around one malformed row, drops rows missing numeric fields, `[]` only for unrecoverable garbage |
| R-035 | **A manager can tell a scoring FAILURE apart from a genuinely un-scorable session.** Each completed session records a `scoring_status` (`SCORED` / `FAILED` / `NOT_SCORABLE`) at end-of-session; the manager reporting export surfaces it as a `Scoring` column so a null/blank score is never ambiguous — the "managers don't trust the /10 scores" kill-signal | live | migration `20260711160000_add_scoring_status` (nullable, additive; applied by the `migrate` job now gating `deploy.yml`); `endSession` (`src/app/api/chat/route.ts`) persists the status; `src/lib/report.ts` `scoringLabel()` maps it to the last CSV + PDF column. Unit `tests/unit/lib/report.test.ts` (E-root #8b): `Failed` ≠ `Not scorable`, pre-instrumentation rows inferred, `Scoring` is the last column |
| R-034 | **A lightly-configured scenario still feels like a real person.** A scenario with a missing or partial `script` falls back field-by-field — a set difficulty/objective/hints is preserved, never discarded by an all-or-nothing reset — and any genuinely absent persona/objective is grounded in the scenario name, not the contentless generic "a customer who needs assistance" | live | `src/lib/ai.ts` — pure `normalizeScript()` fills each field independently with scenario-grounded defaults and always resolves a valid difficulty (guards the latent `undefined.toUpperCase()` crash on a partial script). Unit `tests/unit/lib/ai.test.ts` (E-root #6): preserves set difficulty/objective/hints when persona missing, grounds fallback persona+objective in the scenario name, invalid difficulty → medium, empty scenario name handled, full valid script unchanged |
| R-036 | **The scoring outcome is visible on-screen, not only in the export.** The in-app `/analytics` view surfaces a per-org **Scoring Health** breakdown (Scored / Failed / Not scorable / Unknown) so a manager sees a scoring-engine failure without downloading the CSV/PDF — and is warned explicitly when any session failed to score. Screen and export use the same canonical mapping so they never drift | live | `src/app/api/analytics/route.ts` selects `scoring_status` and counts each session via the shared `scoringLabel()` (`src/lib/report.ts`, widened to a narrow input) into a `scoringHealth` field; `src/app/analytics/page.tsx` renders the breakdown + a red failure warning when `failed > 0`. UI `tests/ui/analytics-page.test.tsx`: breakdown renders from the payload, warns on failures, omits the warning when none failed |

## Non-functional

| ID | Requirement | Status | Verification |
|---|---|---|---|
| R-100 | `/api/health` returns 200 on the branded domain when required env is present | live | `curl https://xpelevator.com/api/health` → 200 (uptime-monitor run 29034316829) |
| R-101 | The Groq scoring credential is valid (the Phase-0 expiry never silently recurs) | live | direct Groq `/v1/models` probe every 15 min (uptime-monitor.yml) |
| R-102 | Health probe every 15 min + scoring canary every 6 h; failure opens one alert issue | live | `uptime-monitor.yml`; run 29034316829 green |
| R-103 | CI blocks merge on typecheck + lint + unit + ui (credential-free) tiers | live | `ci.yml` gates; run 28790934684 (21 ui + 85 unit) |
| R-104 | Coverage ratchets toward 80/85/70 → 90/90/85 floors | building | Vitest coverage; not yet gated |
| R-110 | Deploy secrets managed via org GCP Secret Manager / WIF, not repo secrets | building | blocked on GCP-admin WIF pool binding for this repo (#16 2026-07-06) |
| R-111 | Sentry initialized with sourcemap upload in the deploy workflow | roadmap | Sentry event on a forced error; `docs/SLO.md` present |
| R-112 | PostHog product analytics wired; service-registry + feature-registry entries | roadmap | `feature-registry.yml` present (§11); `/platform/` dashboard reflects it |

## Aspirational (operator / billing / differentiators)

| ID | Requirement | Status | Verification |
|---|---|---|---|
| R-040 | Self-serve operator onboarding → operator workspace that creates + manages client orgs beneath it | building | Phase 4; data-model + management-API foundation shipped as R-048, self-context bootstrap (`GET /api/me`) shipped as R-051, the self-serve **workspace UI** (`/operator`: list + create clients, STANDALONE→OPERATOR onboarding) shipped as R-052, the in-workspace **branding editor** shipped as R-053; remaining: white-label subdomain (R-044) on `@latimer-woods-tech/operator` |
| R-041 | Operator buys seats wholesale via Stripe; sets own retail | roadmap | Stripe test-mode products bound by `lookup_key`; then 🔒 live gate |
| R-042 | Per-seat modality gating (chat / +voice / +phone) enforced from the seat tier | roadmap | gating reads `src/lib/plans.ts`; e2e per tier |
| R-043 | Platform-super-admin vs tenant-admin split for cross-org `/api/orgs/*` governance | roadmap | Phase 4 operator hierarchy |
| R-044 | White-label operator branding (name/logo/colors) + operator subdomain | building | branding data-model + admin management API shipped as R-049, client-facing render surface shipped as R-050, in-workspace branding editor shipped as R-053; remaining: operator subdomain. §17.2 launch rung |
| R-045 | Manager reporting + CSV/PDF export (the artifact operators show clients) | roadmap | Phase 4 |
| R-046 | **Starter scenario-library packs — operators have sellable inventory on day one.** Curated per-vertical packs (a role + a spread of scenarios across difficulty and modality) are shipped as pure, Worker-safe data (`src/lib/scenario-packs.ts`) and served on a public, hidden-mechanic-safe catalog for operators to browse before purchase. The public catalog NEVER exposes a scenario's `script` (persona / objective / hints) — the same Phase-2 boundary as R-021 — so the concealed mechanics stay server-side until an authenticated admin import materialises a pack into org-scoped `scenarios`/`job_titles` (R-047) | live | `GET /api/scenario-packs` (public, no script leak) + `/library` operator surface; unit `tests/unit/lib/scenario-packs.test.ts` + `tests/unit/api/scenario-packs.test.ts` (no `customerPersona`/`customerObjective`/`hints` in the payload), UI `tests/ui/library-page.test.tsx`; deploy gate: anon `/api/scenario-packs` → 200 + no hidden-mechanic keys, anon `/library` → 200 + copy-clean |
| R-047 | **Admin import: a starter pack materialises into the caller's org.** An ADMIN can `POST /api/scenario-packs/import { packId }` to turn a catalog pack (R-046) into usable, org-scoped `job_titles` + `scenarios` rows — the half of "sellable inventory" that makes the catalog actionable. The write is ADMIN-only + strictly tenant-scoped (anon 401, non-admin 403, no-org 400) and **idempotent + non-clobbering**: re-importing a pack never duplicates and never overwrites an operator's later edits (a pack imported for a client stays frozen even if the public pack later improves), and every row is stamped with `source_pack_id` + `pack_version` so drift is detectable by the opt-in "upgrade pack" action (R-054). The import response carries a pure modality/cost **profile** (per-modality counts, latency risk, interruption-handling flag, turn estimate) so an operator sees the operational shape before committing; a `dryRun` preview writes nothing | live | `POST /api/scenario-packs/import` (`buildPackImportPlan` / `packModalityProfile` in `src/lib/scenario-packs.ts`; `ON CONFLICT DO NOTHING` on the org-scoped provenance indexes in migration `20260712120000_add_pack_provenance`); unit `tests/unit/api/scenario-packs-import.test.ts` (401/403/400/404, dry-run, fresh/re-import/partial idempotency, 500) + `tests/unit/lib/scenario-packs.test.ts` (plan + profile); middleware gate `tests/unit/middleware.test.ts` (catalog public, import subpath 401); deploy gate: anon `POST /api/scenario-packs/import` → 401 on both hosts |
| R-048 | **Operator→client hierarchy foundation.** An `OrgKind` (`STANDALONE` / `OPERATOR` / `CLIENT`) plus a self-referential `organizations.parent_org_id` model the channel: an OPERATOR org owns CLIENT orgs beneath it. An ADMIN manages clients via `GET/POST /api/orgs/[id]/clients` — strictly tenant-scoped by `canManageOrgClients` (a platform admin may manage any operator; an operator admin ONLY their own operator's clients — cross-tenant 403), the parent must exist (404) and must not itself be a CLIENT (409, two-level hierarchy), and creating the first client promotes a STANDALONE parent to OPERATOR. Additive/nullable migration → every existing org stays a STANDALONE tenant. The prerequisite org-scoped `job_titles.name` uniqueness (so two clients can hold the same role) landed in R-047's migration. Wholesale billing (R-041) + white-label (R-044) remain later, founder-gated slices | live | `src/lib/org-hierarchy.ts` (pure predicate + slug helpers) + `src/app/api/orgs/[id]/clients/route.ts`; migration `20260712180000_add_org_hierarchy`; unit `tests/unit/lib/org-hierarchy.test.ts` + `tests/unit/api/orgs-clients.test.ts` (401/403 cross-tenant, 400/404/409 guards, promotion, slug-conflict fallback, 500); deploy gate: anon GET + POST `/api/orgs/[id]/clients` → 401 on both hosts |
| R-050 | **Client-facing branding render surface.** The operator's brand (set via R-049) actually SHOWS: a PUBLIC, slug-keyed read `GET /api/branding/[slug]` returns ONLY the brand-safe fields (`toPublicBranding`: slug + displayName / logoUrl / primaryColor / accentColor — never the internal org `name`, `plan`, `parentOrgId`, id, or any tenant/member data), and the sign-in shell (`/auth/signin?org=<slug>`) fetches it to present the operator's name / logo / colors before the trainee authenticates. Public by design (`/api/branding` prefix in `middleware.ts`) — read-only, no write verb under the prefix; the admin write stays the gated `/api/orgs/[id]/branding`. No custom brand / unknown slug → the shell falls back to the platform default (branding is presentation, never a gate). Operator subdomain is a later slice (R-044) | live | `src/lib/branding.ts` (`toPublicBranding` / `hasBranding`, brand-safe projection) + `src/app/api/branding/[slug]/route.ts` (public GET, 404 unknown slug, 500 on error) + `src/app/auth/signin/page.tsx` (best-effort brand fetch + render); unit `tests/unit/lib/branding.test.ts` (projection exposes ONLY brand-safe keys) + `tests/unit/api/branding-public.test.ts` (200 brand-safe / no tenant field / 404 / 500) + `tests/unit/middleware.test.ts` (read public, admin write still gated) + UI `tests/ui/signin-branding.test.tsx`; deploy gate: anon `GET /api/branding/[slug]` → 404 (public, not 401) on both hosts |
| R-049 | **White-label operator branding — data-model + admin management API.** An org carries nullable branding (`brand_display_name` / `brand_logo_url` / `brand_primary_color` / `brand_accent_color`; NULL = fall back to the platform default) so an OPERATOR presents its own brand on the workspace it hands a client. An ADMIN reads/writes it via `GET/PUT /api/orgs/[id]/branding` — strictly tenant-scoped by `canManageOrgBranding` (platform admin: any org; an org's own admin: that org; an operator admin: a CLIENT beneath them — cross-tenant 403), the org must exist (404). PUT validates every field (https-only logo, `#rrggbb`/`#rgb` colors normalized to lowercase, name length-capped); an invalid field fails the whole request (400); a field sent `null`/`""` clears it, an omitted field is left unchanged (partial merge). Additive/nullable migration → every existing org keeps NULL branding. Operator subdomain + the client-facing render surface are later slices (R-044) | live | `src/lib/branding.ts` (pure validators + `canManageOrgBranding` + `mergeBranding`) + `src/app/api/orgs/[id]/branding/route.ts`; migration `20260712200000_add_org_branding`; unit `tests/unit/lib/branding.test.ts` + `tests/unit/api/orgs-branding.test.ts` (401/403 cross-tenant, 404/400 guards, partial-merge write, clear-via-null, 500); deploy gate: anon GET + PUT `/api/orgs/[id]/branding` → 401 on both hosts |
| R-051 | **Self-context bootstrap (`GET /api/me`).** The primitive the operator-workspace UX (R-040) needs before it can render: any authenticated caller reads their OWN identity + org context — `{ user: { id, email, name, role }, org: { id, name, slug, kind, plan, parentOrgId } \| null, canManageClients }`. Strictly self-scoped — the org is looked up by the caller's own `session.user.orgId` (no id is accepted from the request), so no cross-tenant read is possible; a platform admin (no org) gets `org: null` without a DB query. `canManageClients` is derived server-side from the same rule as `canManageOrgClients` (platform admin or OPERATOR-org ADMIN) so the UI never re-encodes tenant authority. `toSelfContext` copies each org field explicitly (no row spread) so a new sensitive `organizations` column can't leak; `Cache-Control: no-store` (per-user). Unblocks the onboarding UX without adding UI — a client can now discover its own org id + kind to call `/api/orgs/[id]/clients` + `/api/orgs/[id]/branding` | live | `src/lib/self-context.ts` (pure `toSelfContext` + `normalizeOrgKind`) + `src/app/api/me/route.ts`; unit `tests/unit/lib/self-context.test.ts` (projection/no-leak/derived-rule) + `tests/unit/api/me.test.ts` (401 anon, own-orgId read, platform-admin no-DB, missing-row → org null, 500) + `tests/unit/middleware.test.ts` (gated, never public); deploy gate: anon `GET /api/me` → 401 on both hosts |
| R-052 | **Operator self-serve workspace UI (`/operator`).** The admin surface that turns the operator→client APIs into a usable workspace (advances R-040). It bootstraps from `GET /api/me` (R-051) and `operatorWorkspaceView` decides the render in ONE place: an ADMIN of an OPERATOR/STANDALONE org lists + creates the CLIENT orgs beneath them (`GET`/`POST /api/orgs/[id]/clients`, R-048); a STANDALONE admin's first client create promotes their org to OPERATOR — this is the "self-serve operator onboarding". A MEMBER or a CLIENT-org admin gets an ineligible notice; a platform admin (no org) is pointed at `/admin`. The view rule is a strict subset of what the API authorises (`canManageOrgClients(org.id, viewer)` still gates every write) so the UI never widens access. The page renders only a shell — every datum comes from an authenticated, tenant-scoped API — and the route is in the `middleware.ts` matcher, so an anonymous visitor is redirected to `/auth/signin` (never served the operator chrome). Org copy rule holds (no user-facing "AI"). Remaining R-040 work: the in-workspace branding editor (write path is R-049) | live | `src/lib/operator-workspace.ts` (pure `operatorWorkspaceView`) + `src/app/operator/page.tsx` + `/operator` in the `middleware.ts` matcher + a home-page entry; unit `tests/unit/lib/operator-workspace.test.ts` (MEMBER/platform-admin/CLIENT/OPERATOR/STANDALONE branches) + UI `tests/ui/operator-page.test.tsx` (list, create+reload, API-error surface, ineligible, 401 sign-in prompt, onboarding CTA, no "AI"); deploy gate: anon `GET /operator` → 3xx to `/auth/signin` on both hosts |
| R-053 | **In-workspace branding editor (`/operator`).** Closes the in-workspace half of R-040: the operator workspace ships a form that loads the org's current white-label brand (`GET /api/orgs/[id]/branding`, R-049), lets an admin edit name / logo / colors (a native swatch picker synced to a free-text `#rrggbb` field), and saves via `PUT`. A blank field clears it (falls back to the platform default). The form validates client-side with `validateBrandingForm`, which REUSES the exact server normalizers (`normalizeBrandName`/`normalizeLogoUrl`/`normalizeHexColor` from `src/lib/branding.ts`) so the UI never accepts a value the API would reject — but the API re-validates and stays the sole authority (`canManageOrgBranding` gates the write; the editor is a strict subset). No new API surface + no new route: the editor lives inside the already-auth-gated `/operator` page and calls the already-gated `/api/orgs/[id]/branding` (its anon GET+PUT → 401 deploy gate from R-049 still covers it). Org copy rule holds (no user-facing "AI"). Remaining R-040/R-044 work: the operator subdomain (host-resolved branding) | live | `src/lib/branding-form.ts` (pure `brandingToForm` + `validateBrandingForm`, reuses R-049 normalizers) + `BrandingEditor`/`ColorField` in `src/app/operator/page.tsx`; unit `tests/unit/lib/branding-form.test.ts` (round-trip, blank→clear, first-error, short-hex expansion) + UI `tests/ui/operator-page.test.tsx` (load brand, PUT+confirm save, client-side invalid-color block with NO PUT, API-error surface, no "AI"); no new deploy gate needed — R-049's anon GET/PUT → 401 + R-052's anon `/operator` → 3xx gates already cover both surfaces |
| R-054 | **Admin upgrade: re-sync a frozen pack to the current catalog version.** The deliberate, opt-in counterpart to the non-clobbering import (R-047). Import is frozen-by-default so a pack materialised at version N stays at N even after the public catalog improves; `POST /api/scenario-packs/upgrade { packId, dryRun? }` lets an ADMIN explicitly re-sync an ALREADY-IMPORTED pack. The pure `buildPackUpgradePlan` derives the drift from the catalog pack + the org's stored provenanced rows (key + `pack_version`): a row whose stored version is `null`/older than `PACK_CATALOG_VERSION` is **updated** in place with the current content + bumped version; a catalog scenario the org lacks is **inserted** (idempotent, under the pack's existing org role); a row already at the version is **unchanged** (a stored version ahead of the catalog is never downgraded); a stored key that has left the catalog is **orphaned** — reported for operator awareness but NEVER deleted. ADMIN-only + strictly tenant-scoped (anon 401, non-admin 403, no-org 400, unknown pack 404); a pack never imported returns a 200 no-op (`imported:false`) that guides the caller rather than silently importing. Only `source_pack_id`-provenanced rows are touched, so a hand-authored scenario is never overwritten. A `dryRun` preview returns the per-scenario drift audit (no hidden mechanics) and writes nothing | live | `POST /api/scenario-packs/upgrade` (`buildPackUpgradePlan` in `src/lib/scenario-packs.ts`; UPDATE stale + `ON CONFLICT DO NOTHING` insert on the R-047 provenance indexes); unit `tests/unit/api/scenario-packs-upgrade.test.ts` (401/403/400/404, not-imported no-op, dry-run, update/insert/unchanged/orphaned accounting, no DELETE, 500) + `tests/unit/lib/scenario-packs.test.ts` (drift plan: unchanged/stale/null/insert/orphaned/no-downgrade/no-leak); deploy gate: anon `POST /api/scenario-packs/upgrade` → 401 on both hosts |
| R-056 | **Admin Scenario Packs surface — the felt operator control over the pack machinery.** Turns the catalog (R-046), import (R-047), and upgrade (R-054) into a usable workspace surface: a new **Scenario Packs** tab in `/admin` reads a status endpoint and shows, per pack, whether this org has imported it and where an opt-in upgrade is available, then materialises the write routes (Import → R-047, Upgrade → R-054) with a confirm + inline result, re-reading status after each write. Before this an operator could only reach the pack routes by hand — the inventory was unreachable from the UI. A new ADMIN-only, strictly org-scoped read `GET /api/scenario-packs/status` reports per-pack import bookkeeping (state `not_imported`/`up_to_date`/`upgrade_available` + drift counts) derived by the pure `computePackStatus`; it carries NO hidden mechanics (never a scenario `script`). Orphaned rows alone never force an upgrade (they are reported, never deleted). Org copy rule holds (no user-facing "AI") | live | pure `computePackStatus` in `src/lib/scenario-packs.ts` (reuses `buildPackUpgradePlan` for drift) + `GET /api/scenario-packs/status` (ADMIN, org-scoped, single provenance read) + `ScenarioPacksTab` in `src/app/admin/page.tsx`; unit `tests/unit/lib/scenario-packs.test.ts` (not-imported / up-to-date / stale / null-version / insert-drift / orphan-only / no-leak) + `tests/unit/api/scenario-packs-status.test.ts` (401/403/400, per-state output, no script leak, 500) + UI `tests/ui/admin-packs.test.tsx` (per-state actions, import/upgrade POST + result, confirm-declined no-op, load-error surface, no "AI"); deploy gate: anon `GET /api/scenario-packs/status` → 401 on both hosts |
| R-057 | **Conversation-latency instrumentation.** The founder's felt-quality signal (#16, 2026-07-13 — "a half-speed sparring session vs. a real-life simulation") made a measurable metric. `/api/chat` now times every live-customer turn: **time-to-first-token** (the gap a trainee perceives before the simulated customer starts replying) and **total** generation time, classified into a felt-speed `tier` (`realtime` <800ms / `acceptable` <2000ms / `slow`) by the pure, Worker-safe `classifyTurnLatency`. The metric ships two ways — a structured `[chat] latency` log line (session prefix + model + timing) and a `timing` object on the terminal `done`/`session_ending` SSE events, so any client or monitor can read it. Instrumentation only: no change to the conversation itself, no new API surface, and no hidden mechanics in the payload. This is the benchmark the Phase-5 speed work (faster customer-turn model R-120, voice STT/TTS R-121) must beat — you can't tune what you don't measure | live | pure `classifyTurnLatency`/`ttftTier` in `src/lib/latency.ts` + `Date.now()` measurement in `src/app/api/chat/route.ts`; unit `tests/unit/lib/latency.test.ts` (tier boundaries, rounding, total≥ttft clamp, NaN/negative normalisation); live verification: the 6h scoring canary (`scripts/phase1-canary.mjs`) now asserts the opener's terminal SSE event carries a numeric `timing` object on `xpelevator.com` |
| R-058 | **Phone-turn latency instrumentation.** Extends R-057's felt-speed metric from chat onto the **Telnyx phone path** — the leg the founder's "half-speed" note was actually about. R-057 proved the chat text turn is real-time (91ms TTFT), which redirected the speed hunt to the voice path. The Telnyx webhook now times each phone turn from the final STT transcript in hand to the reply being dispatched to TTS: **reply-ready** (model generation) and **speak-dispatch** (the full server-controllable gap before the simulated customer can speak), classified by the pure `classifyPhoneTurn`. Because the phone model call is **non-streaming** (no token-level TTFT) the felt-speed tier is derived from `speakDispatchMs`, not reply-ready — a deliberate difference from `classifyTurnLatency`. Surfaced as a structured `[telnyx] latency` log line (session prefix + `leg` opening/reply + **model** + timing) on both the opening and every reply turn, so the phone path's use of the heavy `llama-3.3-70b-versatile` model is visible in the number. The provider-internal legs (Telnyx STT synthesis + TTS time-to-first-audio) are not observable server-side; this measures the leg we own and can tune. Instrumentation only: no change to the call flow, no new API surface, no hidden mechanics | live | pure `classifyPhoneTurn`/`PhoneTurnTiming` in `src/lib/latency.ts` + `Date.now()` measurement in `src/app/api/telnyx/webhook/route.ts` (opening + reply legs); unit `tests/unit/lib/latency.test.ts` (phone leg exposure, dispatch-gap tiering, half-speed→slow, total≥reply clamp, NaN/negative normalisation). Live proof needs a real Telnyx call (🔒 phone-number + per-minute spend); the metric appears in the webhook log on the next live call |
| R-059 | **Difficulty-routed phone-turn model.** Acts on R-058's finding: the Telnyx phone path hard-coded the heavy `llama-3.3-70b-versatile` model on **every** turn, while `/api/chat` already routes easy/medium turns to the ~3× faster `llama-3.1-8b-instant` (keeping 70B only for `hard` scenarios where de-escalation realism is the differentiator). The phone webhook now mirrors that lever — both the opening (`call.answered`) and every reply (`call.transcription`) turn pick the model via the shared `customerModelForDifficulty(resolveScenarioDifficulty(script))`, the exact composition the chat path uses — so easy/medium phone calls shed the 70B latency that R-058 flagged as the prime suspect for the founder's "half-speed" voice feel. Realism for `hard` scenarios is unchanged. The `[telnyx] latency` log line now reports the model actually used, so the tier drop is provable from the log on the next live call. Behaviour-only routing change: no new API surface, no hidden mechanics, no call-flow change | live | reuses `customerModelForDifficulty`/`resolveScenarioDifficulty` in `src/lib/ai.ts` (unit-covered incl. the `{difficulty}`-object composition, `tests/unit/lib/ai.test.ts`); wired at both phone model-call sites in `src/app/api/telnyx/webhook/route.ts`; `typecheck`/`lint`/424 tests green. Live tier proof needs a real Telnyx call (🔒 phone-number + per-minute spend) — the model appears in the `[telnyx] latency` log on the next live call |
| R-060 | **Chat turn-speed badge — the founder-speed number, felt in-UI.** Surfaces R-057's measured chat-turn `timing` (already emitted on the terminal `done`/`session_ending` SSE events but previously dropped by the client) as a small tier-coloured speed badge in the simulator chat header: 🟢 **Real-time** / 🟡 **Responsive** / 🔴 **Slow**, with `Xs to first reply` in the hover title. The badge shows only a completed turn's speed (hidden while a turn is in flight), closing the loop between the R-057/058/059 latency instrumentation and the human judging felt quality — the founder's "half-speed" worry now has an in-product answer. The label/detail logic is pure + Worker-safe and carries no vendor/"AI" token (org copy rule, asserted by a test). Surfacing only: no API surface change, no call-flow change, no hidden scenario mechanics (the badge carries only `tier` + timing, never the scenario `script`/hints) | live | pure `describeLatencyTier`/`latencyBadge` in `src/lib/latency.ts` + `lastTiming` capture in `src/hooks/useChatSession.ts` (SSE `timing` on `done`/`session_ending`) + tier-coloured badge in `src/components/ChatInterface.tsx`, threaded via `src/app/simulate/[sessionId]/page.tsx`; unit `tests/unit/lib/latency.test.ts` (label per tier, no "AI" token, badge detail/tier from a measured timing); `typecheck`/`lint`/428 tests green |
| R-061 | **Strong model reserved for scoring (fast-turn / strong-score split).** Completes the founder-speed arc (R-057 measure chat → R-058 measure phone → R-059 fix phone turns → R-060 surface it) by acting on the OTHER half of the founder-delegate's recommendation (#16, 2026-07-13): *"a cheaper/faster model may be better for most customer turns, with a stronger model reserved for scoring and coaching summaries."* The customer-turn routing already does the first half (8B for easy/medium live turns, 70B only for `hard`, R-059). The end-of-session **scoring judge** was the inversion — it hard-coded the fast `llama-3.1-8b-instant` for the trust-critical /10 scores + coaching justifications. Scoring is async + post-session (NOT on the latency-sensitive turn path), and "managers don't trust the /10 scores" is a documented kill-signal, so `scoreSession` now uses the stronger `llama-3.3-70b-versatile` (`SCORING_MODEL`). Cost stays negligible — one judge call per completed session, not per turn — and the live conversation speed is untouched (customer-turn routing unchanged). Behaviour-only model change: no new API surface, no schema change, no hidden mechanics | live | `SCORING_MODEL` constant + `scoreSession` in `src/lib/ai.ts`; unit `tests/unit/lib/ai.test.ts` asserts the judge call sends `llama-3.3-70b-versatile` (non-streaming), never the 8B tier; the 6h scoring canary (`scripts/phase1-canary.mjs`) continues to assert a NON-NULL score end-to-end on `xpelevator.com` after deploy |
| R-062 | **Upgrade-pack dry-run preview surfaced in the admin Scenario Packs tab.** Closes the preview-before-commit gap between the server-side dry-run (R-054, `POST /api/scenario-packs/upgrade { dryRun: true }` already returns a per-scenario `items` audit + `counts`) and the operator surface (R-056), which previously fired a **blind `confirm()`** — the operator committed an upgrade without seeing which scenarios would change. The **Upgrade** button is replaced by **Preview changes**, which fetches the dry-run audit and renders it inline: a one-line operator summary plus a per-scenario list badged **Update** (with `vN → vM`) / **Add** / **Unchanged** / **Retired (kept)**, then a **Confirm upgrade** / **Cancel** pair. Confirm commits the real (non-dry-run) upgrade with no second blocking dialog; it is disabled when nothing would change. Presentation is pure + Worker-safe and carries no hidden mechanics (the audit is `name` + `action` + version only, never the scenario `script`/hints) and no user-facing "AI" (org copy rule, asserted by a test). The orphaned-row copy states rows are **kept**, preserving R-054's never-delete contract | live | pure `upgradeActionLabel`/`summarizeUpgradeCounts` + `UpgradePreviewCounts` in `src/lib/scenario-packs.ts`; preview-then-commit wiring in `ScenarioPacksTab` (`src/app/admin/page.tsx`) calling the existing dry-run route; unit `tests/unit/lib/scenario-packs.test.ts` (label per action, orphan-kept, no-op/mixed/orphan-note summaries, plural, no "AI"); `typecheck`/`lint`/tests green |
| R-063 | **Operator per-client manager report.** The manager report (R-045, `GET /api/reports/sessions` — CSV + `?format=pdf`) is "the artifact an operator shows their client", but it scoped strictly to the caller's OWN org — and an operator's own org carries no trainee sessions (they live in the CLIENT orgs beneath it), so the channel-model artifact was unreachable. `?clientOrgId=<id>` now re-targets the report at a specific client, authorized by the pure `canAccessOrgReport` (platform admin: any org; an operator admin: only a CLIENT they own — `viewerOrg === target.parentOrgId`; the org's own admin: itself — never another operator's client → 403; unknown id → 404; a MEMBER never exports another org's sessions). Without the parameter, behaviour is unchanged (own-org report). The operator workspace (`/operator`, R-052) surfaces per-client **CSV / PDF** download links on each client row. Strict tenant scoping is preserved — the session query filters on the authorized target org id, so it can never surface another tenant's sessions | live | pure `canAccessOrgReport` in `src/lib/org-hierarchy.ts` + `?clientOrgId` handling in `src/app/api/reports/sessions/route.ts` (404 unknown / 403 cross-operator, then reuses `sessionsToCsv`/`sessionsToPdf`) + per-client report links in `src/app/operator/page.tsx`; unit `tests/unit/lib/org-hierarchy.test.ts` (platform-admin/own-org/operator-owned/cross-operator/member/unrelated-standalone) + `tests/unit/api/reports-sessions.test.ts` (401/403 gate, own-org default scope, unknown 404, owned-client 200 scoped to CLIENT, cross-operator 403, platform-admin any, pdf); the existing anon `GET /api/reports/sessions` → 401 deploy gate (R-045) still covers the anon case |
| R-064 | **Operator portfolio roll-up report.** Builds on the per-client rail (R-063): where `?clientOrgId=<id>` answers "how is THIS client doing?", `?scope=clients` answers "how is my whole book of clients doing?" — one CSV/PDF export spanning EVERY client org beneath the operator, with a leading **Organization** column that attributes each session to its client (the point of a portfolio view). The operator is resolved + authorized by the pure `resolveOperatorRollup` (an operator admin: their OWN clients — an explicit mismatched `operatorOrgId` is a cross-operator attempt → 403; a platform admin: must name `?operatorOrgId=<id>`, else 400; a MEMBER never reaches a roll-up → 403). The session query scopes on `organizations.parent_org_id = <operator>`, so it can only ever surface clients the operator owns — strict tenant isolation preserved, no cross-operator leak. The single-org `REPORT_COLUMNS` and its serializers are left byte-stable; the roll-up is a distinct additive column set (`ROLLUP_COLUMNS`). The operator workspace (`/operator`, R-052) gains an **All clients** CSV / PDF pair, shown once at least one client exists. Org copy rule holds (no user-facing "AI") | live | pure `resolveOperatorRollup` in `src/lib/org-hierarchy.ts` + `rollupSessionsToCsv`/`rollupSessionsToPdf` + `ROLLUP_COLUMNS` in `src/lib/report.ts` + `?scope=clients` handling in `src/app/api/reports/sessions/route.ts` (400 unnamed-platform / 403 cross-operator, `o.parent_org_id`-scoped query) + portfolio links in `src/app/operator/page.tsx`; unit `tests/unit/lib/org-hierarchy.test.ts` (own/matching-param/mismatched-403/platform-400/platform-named/member-403) + `tests/unit/lib/report.test.ts` (Organization-first columns, per-row org, `(unassigned)` null, comma-escape, PDF Portfolio Report) + `tests/unit/api/reports-sessions.test.ts` (operator 200 scoped + Organization column, cross-operator 403, platform 400/200, pdf); the existing anon `GET /api/reports/sessions` → 401 deploy gate (R-045) still covers the anon case |
| R-065 | **Report date window — the operator's "monthly cut".** Extends every mode of the manager report (`GET /api/reports/sessions` — own-org R-045, per-client R-063, portfolio roll-up R-064) with an optional `?since=YYYY-MM-DD` / `?until=YYYY-MM-DD` filter on a session's completion date (`ended_at`). Both bounds are inclusive UTC calendar dates; `until` is applied as an EXCLUSIVE next-day bound so the whole day is covered regardless of time-of-day. The window composes onto the already-authorized tenant-scope fragment (`ss.org_id`/`parent_org_id`), so it can only ever NARROW the authorized session set — tenant isolation is untouched. A malformed date or an inverted window (`since` after `until`) is a clean **400** raised before any scope resolution or DB read. Absent bounds leave the report all-time (behaviour unchanged). The operator workspace (`/operator`, R-052) gains a compact From/To date control that threads the window onto all four export links (portfolio + per-client, CSV + PDF) via `withWindow`; empty = all-time. Org copy rule holds (no user-facing "AI") | live | pure `parseReportWindow` in `src/lib/report-window.ts` (calendar-date validation + next-day exclusive bound) + window composition in `src/app/api/reports/sessions/route.ts` (400 before DB, `ss.ended_at >=`/`< ` appended to the scope `where`) + date-range control + `withWindow` in `src/app/operator/page.tsx`; unit `tests/unit/lib/report-window.test.ts` (all-time/empty/since-only/until-exclusive/full/boundary-rollover/same-day/inverted-400/malformed-400/overflow-400) + `tests/unit/api/reports-sessions.test.ts` (malformed→400 pre-DB, inverted→400 pre-DB, full-window 200 still own-org-scoped with exclusive until, since-only, no-window all-time); the existing anon `GET /api/reports/sessions` → 401 deploy gate (R-045) still covers the anon case |
| R-066 | **Per-turn latency telemetry, persisted.** Completes the founder-speed arc (R-057 measure chat → R-058 measure phone → R-059 fix phone → R-060 surface badge → R-061 strong-score split) by making the measured metric DURABLE. R-057 times every chat turn and R-060 shows it in-UI, but the number lived only in an ephemeral `[chat] latency` log line + a live badge — a slow turn left no historical record, and R-057 itself names this metric "the benchmark the Phase-5 speed work must beat", which is unusable if it evaporates each turn. The CUSTOMER reply row now stores, from the SAME measurement already taken (no new timing, no extra model call, no change to the conversation): `ttft_ms`, `total_ms`, `latency_tier` (realtime/acceptable/slow), the `model` that generated it, and a stable `route_reason` token (`difficulty=…→fast|realism`, R-059) so a slow row explains its own model choice. Additive + nullable columns → AGENT rows and all pre-instrumentation history stay NULL; no API-surface change, no hidden mechanics (telemetry never carries the scenario `script`/hints), org copy rule untouched | live | migration `20260715090000_add_turn_telemetry` (nullable/additive, applied by the `migrate` job gating `deploy.yml`); schema `ChatMessage` telemetry columns + pure `routeReasonForDifficulty` in `src/lib/latency.ts`; persisted at the CUSTOMER `INSERT` in `src/app/api/chat/route.ts` (reusing R-057's `timing` + R-059's `customerModel`); unit `tests/unit/lib/latency.test.ts` (route-reason mapping: hard→realism, easy/medium/unknown→fast); deploy gate `scripts/verify-turn-telemetry.mjs` drives one live turn post-promotion and asserts the reply row's telemetry is non-null on the pages.dev alias |
| R-067 | **Response-speed read surface — the founder-speed number, made visible.** The read-side complement to R-066: R-066 makes per-turn latency DURABLE, but the stored `ttft_ms`/`total_ms`/`latency_tier`/`model`/`route_reason` still lived only in the row — no one could SEE how fast the simulator feels across sessions, which model is the slow leg, or whether the hard-scenario realism route (R-059) is paying its speed cost. `GET /api/analytics/latency` aggregates a tenant's persisted reply-turn telemetry into a summary — mean + p95 time-to-first-token (the perceived gap), the felt-speed tier mix, the %-slow share, and the same split **by model** and **by route-reason** — and the `/analytics` page renders it as a **Response Speed** section (stat cards + tier chips + per-group tables). This is the visible artifact R-066's data was for, and the benchmark any Phase-5 model/voice swap must beat. Access is identical to `/api/analytics`: any authenticated user, strictly org-scoped (`org_id = <org> OR IS NULL`), anon → 401; only aggregate timing is returned — no message content, no scenario `script`/hints, so no hidden mechanic can leak. Org copy rule holds (no user-facing "AI") | live | pure, unit-tested percentile + aggregation in `src/lib/latency-summary.ts` (nearest-rank p95, model/route-reason grouping, `(unknown)` fold, unknown-tier handling); thin auth+query shell `src/app/api/analytics/latency/route.ts` (same tenant filter as `/api/analytics`); **Response Speed** section in `src/app/analytics/page.tsx` (non-blocking sub-fetch, hidden on failure/no-data); unit `tests/unit/lib/latency-summary.test.ts` (percentile nearest-rank/empty/clamp, headline mean/p95/slow%, ms-normalisation, model/route grouping + ordering, `(unknown)` fold, unknown-tier excluded from breakdown but counted in totals) — 96%/100% stmt/line; deploy gate `scripts/verify-latency-summary.mjs` asserts anon→401 then drives one live turn post-promotion and reads the authenticated summary back (measuredTurns≥1, numeric avg/p95, non-empty byModel) on the pages.dev alias |
| R-069 | **Operator portfolio per-client totals (summary view).** The portfolio roll-up (R-064, `?scope=clients`) answers "how is my whole book doing?" but only as one row PER SESSION — an operator with many clients had to hand-total the export to see how each client stands. `?view=summary` (only meaningful with `scope=clients`) collapses that detail to one totals row PER CLIENT org — session count, scored count, and the weighted average POOLED across all of that client's sessions (`sum(score*weight)/sum(weight)`, the same weighting the per-session number and in-app analytics use, extended to the client's whole set) — plus a trailing **TOTAL (all clients)** portfolio grand-total row. It reads the SAME authorized + windowed session set the detail roll-up reads (identical `parent_org_id` tenant scope + the R-065 `?since`/`?until` window compose unchanged), so it can never widen access. The detail columns/serializers (`ROLLUP_COLUMNS`) are left byte-stable — this is a distinct additive column set (`ROLLUP_SUMMARY_COLUMNS`), CSV + `?format=pdf`. Aggregate totals only — no message content, no scenario `script`/hints; org copy rule holds (no user-facing "AI") | live | pure `rollupClientTotals`/`rollupSummaryToCsv`/`rollupSummaryToPdf` + `ROLLUP_SUMMARY_COLUMNS` in `src/lib/report.ts` (pooled weighted average, `(unassigned)` fold, org-name sort, trailing grand total) + `?view=summary` handling in `src/app/api/reports/sessions/route.ts` (reuses the R-064 roll-up scope/auth + R-065 window, `portfolio-summary` filename); unit `tests/unit/lib/report.test.ts` (summary columns, cross-session pooling, scored-vs-session count, `(unassigned)` fold, CSV header+rows+grand total, comma-escape, empty header-only, PDF byte stream) — `report.ts` 100% stmt/func/line; deploy gate extends the anon `GET /api/reports/sessions` → 401 check to `?scope=clients&view=summary` (CSV + PDF) on both hosts |
| R-068 | **Response-speed modality split + date window — "is voice/phone the slow leg, and was it slow this month?"** Extends the R-067 read surface so the founder-speed number is answerable per modality and per period. R-067 split felt speed by model and route-reason but never by conversation channel, and reported all-time only — so "does the phone path lag chat?" and "how did last month cut?" were unanswerable from stored data. `GET /api/analytics/latency` now (a) joins `simulation_sessions.type` and returns a `byModality` split (CHAT/VOICE/PHONE, same per-group stats as byModel), and (b) accepts inclusive `?since`/`?until` `YYYY-MM-DD` bounds — the SAME parser + semantics as the R-065 report window (`parseReportWindow`), filtering on the turn's own `chat_messages.timestamp`; a malformed date or `since` after `until` → 400. The `/analytics` **Response Speed** section renders a **By modality** table (friendly Chat/Voice/Phone labels) + From/To date pickers with a Clear control. Tenant scope, anon→401, and the aggregate-only/no-hidden-mechanics guarantees are unchanged; org copy rule holds (no user-facing "AI") | live | `byModality` group + `modality` field in the pure `src/lib/latency-summary.ts` (reuses `groupBy`, `(unknown)` fold); `src/app/api/analytics/latency/route.ts` adds `parseReportWindow` (shared with R-065) + `ss.type` select + window filter on `cm.timestamp`; `src/app/analytics/page.tsx` **By modality** table + date-window controls (windowed refetch, empty-window message); unit `tests/unit/lib/latency-summary.test.ts` (modality split ordering + `(unknown)` fold); deploy gate `scripts/verify-latency-summary.mjs` asserts byModality attributes the seeded CHAT turn, malformed `?since`→400, `?since=today` includes the fresh turn, `?until=1970-01-01` excludes it — on the pages.dev alias |
| R-070 | **Phone-turn latency telemetry, persisted — populate the PHONE modality bucket.** The R-068 `byModality` split can answer "is voice/phone the slow leg?" only if PHONE turns actually carry telemetry — but until now only the chat path (R-066) persisted `ttft_ms`/`total_ms`/`latency_tier`/`model`/`route_reason`, while the Telnyx phone path *measured* each turn (R-058, the `[telnyx] latency` log) and then threw the number away. So the byModality PHONE bucket was structurally always empty, and the founder's #1 stated pain (the "half-speed" voice feel) stayed unanswerable from stored data. Both phone legs — the opening (`call.answered`) and every reply (`call.transcription`) turn — now stamp their CUSTOMER `chat_messages` row with the SAME felt-speed number the log already reports, mapped through the pure `phoneTurnTelemetry` (`replyReadyMs → ttft_ms` model leg, `speakDispatchMs → total_ms` full dispatch gap, `tier → latency_tier`, plus `model` + the R-059 `route_reason` token). The reply row is saved before its TTS-dispatch total is known (it must exist for the resolved-turn scoring read), so telemetry is written as a follow-up UPDATE once `classifyPhoneTurn` has the full timing; AGENT rows and any turn whose dispatch never completes stay NULL, exactly like the chat path. No call-flow change, no new API surface, no hidden mechanics (telemetry never carries the scenario `script`/hints), org copy rule untouched; reuses R-058's already-taken measurement (no new timing, no extra model call) | live | pure `phoneTurnTelemetry`/`PersistedTurnTelemetry` in `src/lib/latency.ts`; `saveMessage` returns the row id + `stampTurnTelemetry` UPDATE wired at both phone legs in `src/app/api/telnyx/webhook/route.ts`; unit `tests/unit/lib/latency.test.ts` (phone→column mapping, dispatch-gap tier passthrough, model/route-reason passthrough); `typecheck`/`lint`/460 unit tests green. Live PHONE-bucket population needs a real Telnyx call (🔒 phone-number + per-minute spend, same standard as R-058/R-059) — appears in the byModality split on the next live call; the READ side that buckets it is already deploy-gate-verified (R-068) |
| R-120 | LLM routed through `@latimer-woods-tech/llm` on a per-app CF AI Gateway | roadmap | Phase 5; ghost-gateway preflight |
| R-121 | Voice upgrade: Deepgram STT + ElevenLabs TTS behind a provider abstraction | roadmap | Phase 5 |
| R-130 | User-facing copy uses craft vocabulary, never the word "AI" | building | copy-pass (Phase 4); unit guard already blocks "AI" in plan copy |

## Rejected (do-not-build guardrails)

| ID | Requirement | Status | Reason |
|---|---|---|---|
| R-900 | Retail / consumer self-serve signup | rejected | Channel-first: we sell to operators, not individuals (founder 2026-07-05) |
| R-901 | End-customer marketing / retail pricing site | rejected | Only an operator-facing demo/wholesale surface exists |
| R-902 | Deep Telnyx-specific coupling | rejected | Bandwidth swap is planned; keep telephony behind a provider seam |
| R-903 | Multiplayer role-play / cohort analytics / adaptive difficulty now | parked | Parked until paying tenants exist — revisit, don't build |
